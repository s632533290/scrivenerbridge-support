# ScrivenerBridge public pages

GitHub Pages source for the ScrivenerBridge marketing, support, and privacy-policy page.

Every App Store locale is selected through `?lang=<locale>`. The support section uses `#support`; the marketing section uses `#marketing`.
